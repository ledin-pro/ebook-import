[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "pro-ledin-ebook-import"
version = "0.1.0"
description = "Deterministic EPUB to Obsidian Markdown importer for AI-agent-readable book sources."
readme = "README.md"
requires-python = ">=3.10"
license = "MIT"
license-files = ["LICENSE"]
authors = [{ name = "mxl" }]
keywords = ["epub", "obsidian", "markdown", "ebooks", "ai-agents"]
classifiers = [
    "Development Status :: 4 - Beta",
    "Environment :: Console",
    "Intended Audience :: End Users/Desktop",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.10",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Programming Language :: Python :: 3.13",
    "Programming Language :: Python :: 3.14",
    "Topic :: Text Processing :: Markup :: Markdown",
]
dependencies = []

[project.optional-dependencies]
dev = ["pytest>=8.0"]

[project.scripts]
ebook-import = "pro.ledin.ebook_import.core:main"

[project.urls]
Homepage = "https://github.com/ledin-pro/ebook-import"
Repository = "https://github.com/ledin-pro/ebook-import"

[tool.hatch.build.targets.wheel]
packages = ["src/pro"]

[tool.hatch.build.targets.sdist]
include = [
    "src/pro",
    "tests",
    "evals",
    "skills/ebook-import/SKILL.md",
    "README.md",
    "LICENSE",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["src"]
